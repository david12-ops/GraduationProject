import Head from 'next/head';

import { useTeamQuery } from '@/generaze-graph';

import styles from '../styles/Home.module.css';
import { SearchAppBar } from './components/navbar';
import { FullBorderedGridOfHomePage } from './components-of-home/layout-of-first-page';

export default function Home() {
  const dataOfTeam = useTeamQuery({
    variables: { team: '60', season: '2020', league: '39' },
  });

  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <SearchAppBar />

      <main className={styles.main}>
        <FullBorderedGridOfHomePage />
        {console.log(dataOfTeam.data?.teams)}
      </main>

      <footer className={styles.footer}></footer>
    </div>
  );
}
